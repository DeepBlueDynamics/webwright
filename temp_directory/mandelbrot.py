"# Importing necessary libraries\nimport numpy as np\nimport matplotlib.pyplot as plt\n\n# Defining the Mandelbrot function\ndef mandelbrot(c, max_iter):\n    z = 0\n    n = 0\n    while abs(z) <= 2 and n < max_iter:\n        z = z*z + c\n        n += 1\n    return n\n\n# Displaying the Mandelbrot set\nwidth, height, max_iter = 800, 800, 100\nr1 = -2\nr2 = 1\ni1 = -1\ni2 = 1\n\n# Generating the image\nimage = np.zeros((height, width))\nfor x in range(width):\n    for y in range(height):\n        zx, zy = x * (r2 - r1) / (width - 1) + r1, y * (i2 - i1) / (height - 1) + i1\n        image[y, x] = mandelbrot(complex(zx, zy), max_iter)\n\n# Plotting the image\nplt.imshow(image, cmap='hot', extent=(r1, r2, i1, i2))\nplt.colorbar()\nplt.title('Mandelbrot Set')\nplt.show()" 
